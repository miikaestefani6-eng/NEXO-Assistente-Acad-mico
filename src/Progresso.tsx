import { useMemo, useState } from "react";
import { getAcademicProgress, getDisciplineProgress } from "./lib/nexo/progress";
import { loadAcademicState } from "./lib/nexo/academicState";

function Progresso() {
  const [selected, setSelected] = useState<string | null>(null);
  const disciplines = loadAcademicState();
  const overall = useMemo(() => getAcademicProgress(disciplines), [disciplines]);
  const urgent = disciplines.reduce((current, discipline) => discipline.daysUntilExam < current.daysUntilExam ? discipline : current, disciplines[0]);

  return <div className="app"><header className="topbar"><div className="brand-area"><span className="brand">NEXO</span><span className="brand-subtitle">Assistente Acadêmico</span></div><nav className="desktop-nav"><a href="/">Hoje</a><a href="/agenda">Agenda</a><a href="/disciplinas">Disciplinas</a><a className="active" href="/progresso">Progresso</a></nav><div className="student"><div className="avatar">E</div><div className="student-info"><strong>Estudante</strong><span>Meu semestre</span></div></div></header><main className="main-content"><section className="welcome"><div><p className="eyebrow">VISÃO ACADÊMICA</p><h1>Você está avançando.<br />O NEXO mostra <em>quanto.</em></h1><p className="welcome-text">Seu progresso não é só uma porcentagem. É a distância entre onde você está e o que precisa estar pronto para as próximas provas.</p></div></section><section className="progress-container"><div className="progress-hero"><div><span className="eyebrow">PROGRESSO GERAL</span><div className="progress-number">{overall.percent}<span>%</span></div><p>{overall.done} de {overall.total} itens concluídos · {overall.pending} pendentes.</p></div><div className="progress-ring"><span>{overall.percent}%</span></div></div><div className="insight-card"><span className="insight-icon">🧠</span><div><strong>O que o NEXO percebe</strong><p>{urgent?.name} exige atenção primeiro: é a disciplina com a prova mais próxima.</p></div><a href="/disciplinas">Ver plano →</a></div><div className="section-title"><div><p className="eyebrow">POR DISCIPLINA</p><h2>Seu semestre, de verdade.</h2></div></div><div className="progress-grid">{disciplines.map((d) => { const p = getDisciplineProgress(d); return <article className={`progress-card ${selected === d.code ? "selected" : ""}`} key={d.code} onClick={() => setSelected(selected === d.code ? null : d.code)}><div className="progress-card-head"><div><span className="task-type">{d.code}</span><h3>{d.name}</h3></div><span className={`risk-dot ${d.daysUntilExam <= 14 ? "risk-high" : d.daysUntilExam <= 21 ? "risk-medium" : "risk-low"}`}>{d.daysUntilExam <= 14 ? "URGENTE" : d.daysUntilExam <= 21 ? "ATENÇÃO" : "ESTÁVEL"}</span></div><div className="progress-bar"><span style={{ width: `${p.percent}%` }} /></div><div className="progress-card-meta"><strong>{p.percent}%</strong><span>{p.done} de {p.total} concluídos</span><span>{p.pending} pendentes</span></div><div className="progress-exam"><span>PRÓXIMA PROVA</span><strong>{d.exam}</strong><small>em {d.daysUntilExam} dias</small></div>{selected === d.code && <div className="progress-detail"><strong>Leitura do NEXO</strong><p>{d.daysUntilExam <= 14 ? "Priorize esta disciplina na sua próxima sessão. O objetivo é reduzir o atraso sem sacrificar as outras matérias." : d.daysUntilExam <= 21 ? "Mantenha o ritmo e use as próximas sessões para transformar pendências em avanço consistente." : "Situação sob controle. Continue avançando e preserve espaço para revisão antes da prova."}</p></div>}</article>; })}</div></section></main><button className="assistant-button" onClick={() => alert("O Assistente de Estudos será conectado à IA.")}><span className="assistant-sparkle">✨</span><span>Preciso de ajuda</span></button></div>;
}
export default Progresso;
